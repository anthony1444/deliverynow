import { SelectCustomComponent } from './select-custom.component';

describe('SelectCustomComponent', () => {
  let component: SelectCustomComponent;

  beforeEach(() => {
    component = new SelectCustomComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
